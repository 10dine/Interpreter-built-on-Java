define fib (x : integer)
	variables a, b, c, i :integer

	a:=1
	b:=1
hello
	hello
		"hello"
			'c'
		float
	real
boolean

	write a
	write b
	
	for i from 2 to 12
	
		c:= a + b
		a:=b
		b:=c
		
		write c

define gcditer(a, b : integer)

	variables R :integer 

	while (a mod b) > 0  
		R := a mod b
		a := b
		b := R
	
	write R

define gcdrecur(a, b : integer)
	if b = 0 then
		write a
	else
		gcdrecur(b, (a mod b))

define main()
	fib(12)

	
	