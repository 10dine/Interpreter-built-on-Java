define fib (x : integer)
	variables a, b, c, i :integer

	a:=1
	b:=1

	variables x :real
	x:=4.90
hello
	hello
		"hello"
			'c'
		float
	real
boolean

	write a
	write b
	
	for i from 2 to 12
	
		c:= a + b
		a:=b
		b:=c
		
		write c

define gcditer(a, b : integer)

	variables R :integer 

	while (a mod b) > 0  
		R := a mod b
		a := b
		b := R
	
	write R

define gcdrecur(a, b : integer)
	if b = 0 then
		write a
	else
		gcdrecur(b, (a mod b))

define main()
	fib(12)

	"pop"
	'pop'78.9vov:=:=*mod

	1
        a
            b
        c

    {this is a multi-line comment
                haiii :3 haiii}
    5 - 3
    name[5]
    while 5>= 3
    for 5<>i
    1.2.3 "ezpz " "c" "" for while if then else